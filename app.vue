<template>
  <NuxtLayout>
    <NuxtPage />
  </NuxtLayout>
</template>

<!-- <style lang="scss">
@import url('https://cdn.jsdelivr.net/npm/@acab/reset.css@0.5.3/index.min.css');
@import url('https://fonts.googleapis.com/css2?family=Figtree:ital,wght@0,300;0,400;0,600;0,700;1,400&family=Josefin+Sans:wght@400;600;700&display=swap');

:root {
  --step--2: clamp(0.78rem, calc(0.77rem + 0.03vw), 0.80rem);
  --step--1: clamp(0.94rem, calc(0.92rem + 0.11vw), 1.00rem);
  --step-0: clamp(1.13rem, calc(1.08rem + 0.22vw), 1.25rem);
  --step-1: clamp(1.35rem, calc(1.28rem + 0.37vw), 1.56rem);
  --step-2: clamp(1.62rem, calc(1.50rem + 0.58vw), 1.95rem);
  --step-3: clamp(1.94rem, calc(1.77rem + 0.87vw), 2.44rem);
  --step-4: clamp(2.33rem, calc(2.08rem + 1.25vw), 3.05rem);
  --step-5: clamp(2.80rem, calc(2.45rem + 1.77vw), 3.82rem);

  --primary-color: #D4145A;
  --accent-color: #FBB03B;
}

body {
  background: #fbf9f3;
  color: #333;
  font-family: 'Figtree', sans-serif;
  font-size: var(--step-0);
  accent-color: var(--accent-color);
}

main {
  width: min(100% - 3rem, var(--container-max, 120ch));
  margin-inline: auto;
}

h1,
h2,
h3 {
  font-family: 'Josefin Sans', sans-serif;
  font-weight: bold;
  color: var(--primary-color);
}

h4{
  font-size: var(--step-0);
}

h3 {
  font-size: var(--step-1);
}

h2 {
  font-size: var(--step-2);
}

h1 {
  font-size: var(--step-3);
}

a {
  color: var(--primary-color);
}

input, select {
  background: white;
}

button,
input[type="button"],
input[type="submit"] {
  background: var(--primary-color);
  color: white;
  padding-inline: 0.5ch;
  border: none;
  border-radius: 0.25rem;
}

.flow {
  >* {
    margin: 0;
  }

  >*+* {
    margin-block-start: var(--flow-space, 1.5rem);
  }
}
</style> -->